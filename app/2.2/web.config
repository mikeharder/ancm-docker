<?xml version="1.0"?>
<configuration>
  <!-- Temporarily required to enable v2 ANCM -->
  <system.webServer>
    <handlers>
      <add name="aspNetCore" path="*" verb="*" modules="AspNetCoreModuleV2" resourceType="Unspecified" />
    </handlers>
    <aspNetCore processPath="%LAUNCHER_PATH%" arguments="%LAUNCHER_ARGS%" forwardWindowsAuthToken="false" stdoutLogEnabled="true" stdoutLogFile="C:\logs\stdout">
      <handlerSettings>
        <handlerSetting name="debugLevel" value="4" />
        <handlerSetting name="debugFile" value="C:\logs\debug.txt" />
      </handlerSettings>
      <environmentVariables>
        <!-- Test unused environment variable -->
        <environmentVariable name="FOO" value="BAR" />

        <!-- Uncomment to run on fixed port instead of random port -->
        <!-- <environmentVariable name="ASPNETCORE_PORT" value="10000" /> -->
      </environmentVariables>
    </aspNetCore>
  </system.webServer>
</configuration>
