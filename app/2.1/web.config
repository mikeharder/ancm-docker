<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>
    <handlers>
      <add name="aspNetCore" path="*" verb="*" modules="AspNetCoreModule" resourceType="Unspecified" />
    </handlers>
    <aspNetCore processPath="%LAUNCHER_PATH%" arguments="%LAUNCHER_ARGS%" stdoutLogEnabled="true" stdoutLogFile="C:\logs\stdout">
      <environmentVariables>
        <!-- Test unused environment variable -->
        <environmentVariable name="FOO" value="BAR" />

        <!-- Uncomment to run on fixed port instead of random port -->
        <!-- <environmentVariable name="ASPNETCORE_PORT" value="10000" /> -->
      </environmentVariables>
    </aspNetCore>
  </system.webServer>
</configuration>
